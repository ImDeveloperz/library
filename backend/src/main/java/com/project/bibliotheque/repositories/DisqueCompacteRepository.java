package com.project.bibliotheque.repositories;

import com.project.bibliotheque.entities.DisqueCompacte;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DisqueCompacteRepository extends JpaRepository<DisqueCompacte, Long> {
}
